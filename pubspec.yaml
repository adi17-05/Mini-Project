name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  # (Optional) For chatbot or dialog UI
  flutter_chat_ui: ^2.6.1
  # Charts and UI enhancements
  fl_chart: ^0.69.0
  google_fonts: ^6.2.1
  animated_text_kit: ^4.2.2
  percent_indicator: ^4.2.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
